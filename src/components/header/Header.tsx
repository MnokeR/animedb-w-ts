import DarkMode from "./DarkMode";
import HeaderWrapper from "./HeaderWrapper";
function Header() {
  return (
    <HeaderWrapper>
      <DarkMode />
    </HeaderWrapper>
  );
}

export default Header;
